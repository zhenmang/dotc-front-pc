import { ethers } from 'ethers'
// export const gasOpt = { gasLimit: 2000 * 10000 + '', gasPrice: ethers.utils.parseUnits('10', 'gwei') }
// export const gasOpt = { gasPrice: ethers.utils.parseUnits('10', 'gwei') }
export const gasOpt = { gasLimit: 2000 * 10000 + '' }

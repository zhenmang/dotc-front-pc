import store from '@/store'
import { ethers } from 'ethers'
import { getContracts } from '@/contract/contracts.js'
// import { useI18n } from 'vue-i18n'
// const { t } = useI18n()

export default async function queryTokenUsdPrice() {
    // const dotcRead = getContracts().dotcIns.read
    // let pledgeRate = await dotcRead.getPledgeRate()
    // if (typeof pledgeRate === 'string' && pledgeRate.includes('%')) { 
    //     parseFloat(pledgeRate)
    // }
    // store.commit('updatePledgeRate', pledgeRate)
}
